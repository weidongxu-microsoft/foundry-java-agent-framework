# Foundry hosted agent — port 8088 is the platform's expected container port.
server.port=8088

# Model + OpenAI-compatible endpoint for the vision photo-process (crop) advice call.
# MODEL=gpt-4o-mini
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
