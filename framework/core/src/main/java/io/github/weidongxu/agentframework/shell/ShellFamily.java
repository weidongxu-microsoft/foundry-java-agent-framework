package io.github.weidongxu.agentframework.shell;

/** The idiom family of a shell, used to steer command syntax. */
public enum ShellFamily {
    /** Windows PowerShell / pwsh. */
    POWERSHELL,
    /** POSIX shells (bash, sh, zsh). */
    POSIX
}
