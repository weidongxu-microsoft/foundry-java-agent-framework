# 03 — Workflows

Workflow orchestration patterns, mirroring MAF's `03-workflows/`. Backed by `framework/workflows`.

> **Status:** scaffold — no code yet. Planned examples below.

| Pattern | Framework surface |
|---------|-------------------|
| sequential | `workflows/workflow/*` |
| concurrent | `workflows/workflow/*` |
| handoff | `workflows/workflow/*` |
| group-chat | `workflows/workflow/*` |

**N/A for now** (see `plan/11`): declarative (YAML) workflows, checkpoints / durable execution.
