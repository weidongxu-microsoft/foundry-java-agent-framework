# 05 — End-to-end

Full applications and demos that combine multiple framework features, mirroring MAF's
`05-end-to-end/`.

> **Status:** scaffold — no code yet.

The existing `app/` (hosted agent: web search + memory + tools/skills) paired with `client/`
(invokes the deployed agent and verifies its features) is the current end-to-end reference. Curated
end-to-end samples will distill that into focused, self-contained walkthroughs.
